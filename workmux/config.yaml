nerdfont: true

# Pane layout for new worktree windows. <agent> runs the configured agent
# (default: claude). Required when using `workmux add -p "<prompt>"`.
panes:
  - command: <agent>
    focus: true
